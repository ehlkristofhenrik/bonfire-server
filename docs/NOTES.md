* Cargo missing feature for per-bin dependencies -> defaulting to shared deps

Bonfire-server reads the following configuratons:

- GITLAB_API_KEY environment variable
- config.json file ( see static/config.json for more info )
